<!-- <script setup>
import { ref, onMounted } from 'vue'

const token = ref('')

onMounted(() => {
  const savedToken = localStorage.getItem('jwt_token')
  if (savedToken) {
    token.value = savedToken
  }
})
</script>

<template>
  <div class="p-4 bg-gray-100 rounded-xl">
    <h2 class="text-lg font-bold mb-2">Token từ localStorage:</h2>
    <p class="break-words text-sm text-gray-800">{{ token }}</p>
  </div>
</template> -->
